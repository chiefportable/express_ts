<h1> {{message}} </h1>
{{! passing a comment in handlebars }}
<div>
    <img src="img/home.jpg" alt="" srcset="">
</div>

<ul>
    {{#each tours}}
        <li>
            {{name}} - {{price}}
            {{#if ../currencies}}
               {{../currency.name}}
            {{/if}}
        </li>
    {{/each}}
</ul>

{{#unless currencies}}
    <p>All prices in {{currency.name}} </p>
{{/unless}}
